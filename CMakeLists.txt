cmake_minimum_required(VERSION 2.8)
project(control_subsystem)

find_package(catkin REQUIRED)

catkin_package()

#catkin_package(
#  LIBRARIES ${PROJECT_NAME}_solver
#  DEPENDS roscpp rosconsole rostime tf tf_conversions kdl_parser orocos_kdl
#)

